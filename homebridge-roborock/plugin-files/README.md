# Synced Plugin Files

This directory is populated from the live Pi installation by:

```sh
homebridge-roborock/scripts/sync-patched-plugin-files.sh
```

The live patched plugin files are not copied yet because the Pi is unreachable
from the current network.
